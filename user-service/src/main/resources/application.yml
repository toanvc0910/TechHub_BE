server:
  port: 8080

spring:
  application:
    name: USER-SERVICE
  profiles:
    active: prod
  datasource:
    url: jdbc:postgresql://localhost:5432/techhub
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: com.techhub.app.userservice.config.PostgreSQLEnumDialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: trungphandinh340@gmail.com
    password: zqik zowr vngn cgsx
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  # OAuth2 Configuration for Social Login
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 277346786159-ldes3jasve2esa38ln4vro3dhkogvtrf.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile
          facebook:
            client-id: your-facebook-app-id
            client-secret: your-facebook-app-secret
            scope:
              - email
              - public_profile
          github:
            client-id: Ov23li8nzLrd782hJFCl
            client-secret: ea5864eebec0623ad13a2d1f61f61a6df9b8e7bc
            scope:
              - user:email
              - read:user

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    hostname: localhost
    prefer-ip-address: true

logging:
  level:
    com.techhub.app.userservice: DEBUG
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# App specific settings
app:
  oauth2:
    # Frontend URL to redirect after successful OAuth2 login
    authorized-redirect-uris: ${APP_OAUTH2_REDIRECT_URI:http://localhost:3000/oauth2/redirect}
