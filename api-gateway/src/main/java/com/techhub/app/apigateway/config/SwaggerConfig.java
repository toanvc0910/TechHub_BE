package com.techhub.app.apigateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Swagger configuration for API Gateway
 * Swagger is configured in application.yml to aggregate all microservices
 */
@Configuration
public class SwaggerConfig {
    // Configuration is done in application.yml
    // This class is just a placeholder for future customizations
}
